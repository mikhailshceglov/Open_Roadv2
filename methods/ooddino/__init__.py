"""OoDDINO: two branches, fused at the threshold.

    method.py     the MethodSpec the skeleton loads
    pipeline.py   the cascade, from logits and proposals to instances
    maths.py      the pixel branch's arithmetic -- pure numpy, no models
    boxes.py      boxes, blobs, and the filters between them
    models.py     GroundingDINO, SegFormer-B2, SAM 2.1
"""
