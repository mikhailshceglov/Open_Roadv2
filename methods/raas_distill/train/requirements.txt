# The training pipeline, on top of ../requirements.txt. Not needed to run the
# released weights — inference imports nothing from this directory.

-r ../requirements.txt

boto3>=1.34          # Object Storage transfer in s3sync.py
h5py==3.11.0         # the official SMIYC prediction format
scipy==1.10.1        # Spearman teacher-fidelity in stages/evaluate.py
onnx==1.16.2         # stages/export.py
onnxruntime==1.19.2

# Not installable from PyPI, and not vendored here: the labelling and
# evaluation stages also need the RAAS monorepo (detectron2, Mask2Former,
# Maskomaly, a Swin-L checkpoint). Point $RAAS_ROOT at a checkout of it.
