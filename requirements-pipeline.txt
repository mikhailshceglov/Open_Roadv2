# Additional dependencies for the training pipeline (stages/), on top of
# requirements.txt.  Not needed to run the released weights.
-r requirements.txt
boto3>=1.34          # Object Storage transfer in s3sync.py
h5py==3.11.0         # official SMIYC prediction format
scipy==1.10.1        # Spearman fidelity in stages/evaluate.py
scikit-learn>=1.3    # metric helpers
onnx==1.16.2         # stages/export.py
onnxruntime==1.19.2
